# Screenshots

Placeholders — no interactive browser session was available in this build
environment to capture real screenshots. Add these when running the demo:

- `dashboard.png` — the OpsPulse dashboard (`http://localhost:8080/`)
- `grafana.png` — the Grafana "OpsPulse Overview" dashboard
- `incident-ai.png` — an AI Incident Assistant response
